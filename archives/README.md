# Archives

This directory contains files that are no longer actively used by the Operator OS application but are preserved for historical context and reference.

## Contents

### _start/
Repository initialization artifacts from the original project setup. Includes the Claude Code starter kit template, design agent configuration, and initialization protocol document. These were used during initial scaffolding and are no longer needed.

- `claude-code-starter-kit.zip` - Original starter template
- `@Design-Agent.xml` - Design agent configuration
- `NEW.txt` - Initialization protocol document

### assets/
Media files that were part of the original repository but are not referenced by any active documentation or code.

- `arch.jpg` - Architecture diagram (unreferenced)
- `compare.jpg` - Comparison chart (unreferenced)
- `licheervnano.png` - Hardware reference image (unreferenced)
- `termux.jpg` - Terminal emulator screenshot (unreferenced)
- `operator_mem.gif` - Duplicate of operator_memory.gif (unused variant)
- `operator_scedule.gif` - Schedule demo GIF (unreferenced, contains typo in filename)
- `agent-icon.png` - Agent icon (unreferenced in code or docs)

### scripts/
Development and testing scripts not part of the CI/CD pipeline or build process.

- `test-docker-mcp.sh` - Manual Docker MCP integration test script

## Date Archived
2026-03-16

## Notes
Active assets retained in `/assets/`: `logo.png`, `operator_code.gif`, `operator_memory.gif`, `operator_search.gif` (all referenced in README.md).
